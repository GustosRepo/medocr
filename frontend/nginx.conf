server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Simple SPA routing
  location / {
    try_files $uri /index.html;
  }

  # API proxy: when containers run on a user network this resolves to the
  # backend service name. This is the recommended production/dev compose
  # configuration so containers can address each other by service name.
  location /api/ {
    proxy_pass http://api:4387/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
  }

  location /dashboard {
    proxy_pass http://api:4387/dashboard;
  }

  # Static assets served directly
  location /static/ {
    proxy_pass http://api:4387/static/;
  }
}
